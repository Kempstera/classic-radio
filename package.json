{
  "name": "classic-radio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "颂 · A classical radio listening room",
  "scripts": {
    "dev": "python3 -m http.server 4173 --bind 127.0.0.1 --directory public",
    "test": "node --test scripts/player.test.js",
    "check": "node scripts/check.mjs",
    "deploy": "node scripts/deploy.mjs"
  }
}
